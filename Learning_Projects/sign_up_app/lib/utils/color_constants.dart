import 'package:flutter/material.dart';

class ColorConstants {
  static const primary = Color(0xFFF9D949);
  static const secondary = Color(0xFF393646);
  static const white = Colors.white;
  static const errorColor = Colors.red;
  static const yellowBgColor = Color.fromARGB(176, 241, 205, 42);
  static const kLabelTextColor = Color.fromRGBO(39, 40, 60, 0.5);
}
